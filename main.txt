git --version // for git version

git init //Initialized empty Git repository

git add * // added all file

git commit -m "first commit" // commit code with message

git branch -M main //create main branch with name main

git remote add origin <remote reposotory link> // added remote reposotory as origin 

git push -u origin main // push commited file to remote reposotory

git branch // listing all branch

git checkout <branch-name> // for selecting branch for editing.
 
git branch -m <new_name> // for changing branch name but u have to select branch first.


